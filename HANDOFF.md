# DX 26FW 선제 커뮤니케이션 관제탑 — 작업 인계 문서

> **이 문서를 먼저 끝까지 읽고 작업을 시작할 것.**
> 이 프로젝트는 **논리·방법론은 이미 확정**됐고, **비주얼 디자인만 다시** 해야 하는 상태다.
> 아래 2장(방법론)은 여러 차례 논쟁 끝에 합의된 설계라 **임의로 바꾸지 말 것.** 3~6장은 현재 구현 상태와 재작업 지시다.
> 대상 파일: `dx-26fw-control-tower.html` (단일 HTML, 외부 의존성 0). 같은 디렉터리에 있으면 함께 읽을 것.

---

## 0. 지금 시켜야 할 것 (요약)

- 현재 프로토타입(`dx-26fw-control-tower.html`)의 **비주얼 디자인이 만족스럽지 않다.** 이걸 다시 디자인한다.
- **유지할 것:** 정보 구조(4개 섹션), 판정 로직, 상태 전환 인터랙션, 데이터 스키마(`CATS` 객체), 신호등 색의 의미론.
- **바꿀 것:** 팔레트·타이포·레이아웃 감도·여백·컴포넌트 생김새 등 미감 전반.
- 데이터는 전부 목업이다. 실제 수치는 **지식그래프에서 Claude Code CLI로** 나중에 연결한다(이건 쉬운 일이라 우선순위 낮음).

---

## 1. 프로젝트 개요

- **무엇:** 디스커버리 익스페디션(DX)의 26FW(2026 F/W) 패딩·다운 시즌 마케팅 **"관제탑" 대시보드.**
- **목적:** "선제 커뮤니케이션이 매출을 얼마나 냈나"를 재는 게 **아니다.** **"우리가 선제 커뮤니케이션을 제대로 된 타이밍·방법으로 했나"를 시즌 도중에 판정**하는 계기판이다. 성적표가 아니라, 12월에 결과가 나오기 전에 궤도를 수정하는 도구.
- **배경:**
  - 올해 브랜드 예산이 4년 최저(약 41.7억, 전년비 −48.9%). 돈이 적으니 아무 때나 못 쓴다.
  - 전략 = **선제 릴레이**: 수요가 깨어나는 시점("태동일")보다 앞서서 **경량 → 숏 → 미드 → 롱·방한화** 순으로 릴레이하듯 커뮤니케이션한다.
  - 문제 1: 이 전략은 타이밍이 전부인데, 맞았는지는 시즌이 끝나야(12월) 알 수 있다.
  - 문제 2: **올해는 작년보다 커뮤니케이션을 2개월 앞당겼다.** → 이게 비교 방식을 통째로 바꾼다(2장 참고).
- **대상 사용자:** 마케팅 기획자 + CEO. CEO가 3초 안에 상태를 읽어야 한다.
- **UI 언어:** 한국어.

---

## 2. 핵심 방법론 — 확정됨, 바꾸지 말 것

이 프로젝트의 지적 핵심. 각 결정에는 이유가 붙어 있다. 이유를 이해하고 유지할 것.

### 2.1 판정축 = "브랜드 선호" (매출 아님)
선제의 목표는 "사람들이 사려고 마음먹는 순간, 이미 우리 브랜드를 알고 있게" 만드는 것. 그러니 성패의 본질은 **"수요가 터진 시점에 브랜드 선호가 미리 쌓여 있었나"**다.
- 선호는 직접 관측 불가 → **프록시 신호로 측정:** ① 브랜드 검색량, ② 내부 상품 조회수, ③ 검색 점유율(SoS, 전체 패딩 검색 중 우리 비중).

### 2.2 비교 기준 = "작년의 우리" — 단, 태동일 0점 정렬
- 비교 상대는 경쟁사가 아니라 **작년의 우리**.
- **함정:** 올해 2개월 앞당겼으므로 **달력 날짜로 맞대면**(올해 9월 vs 작년 9월) 작년엔 아무것도 안 하던 시기와 비교하는 것 → 무조건 이긴다. 이건 "선제가 효과 있다"가 아니라 "작년엔 안 하던 걸 했다"는 확인일 뿐.
- **해결:** 달력이 아니라 **'태동일'을 0점으로 맞춰 비교.** 작년·올해의 태동일을 겹쳐놓고 그 지점의 브랜드 선호를 비교한다. → 화면엔 **달력 보기 / 태동일 정렬** 토글 둘 다 제공(달력=사람이 읽는 기본, 태동정렬=공정 YoY 비교).
- 태동일 산출: 감이 아니라 과거 4년 검색·기온 데이터로. (카테고리 검색이 최근 평균 대비 +30% 이상인 상태가 2주 연속 = 태동)

### 2.3 계절·날씨 왜곡 제거 = 카테고리 전체 수요 대비 우리 몫
따뜻한 겨울이면 시장 전체가 죽어 우리도 나빠 보인다. → 특정 경쟁사를 지목하지 않고 **"형성된 전체 패딩 수요 파이에서 우리 비중(SoS)이 커졌나"**만 본다.

### 2.4 【핵심】 두 시점 교차 판정 — 태동 × 피크
대시보드의 심장. 태동 한 시점만 보지 않고 **피크(성수기 정점) 시점도 함께** 본다. 두 지점에서 각각 작년 대비 브랜드 선호가 몇 %p 앞섰는지 재고 교차한다.

| 태동 | 피크 | 판정 | 의미 |
|---|---|---|---|
| ○ | ○ | **성공** | 미리 심은 인지가 성수기까지 살아남아 우위로 이어짐 |
| ○ | ✗ | **경고** | 초반엔 앞섰는데 정작 지갑 열 때 밀림 → **선제 전략 자체를 의심**하는 신호 |
| ✗ | — | **선제 실패** | 미리 자리 잡기에 실패 |

- 가운데 칸(태동○·피크✗)이 그동안 잡기 어렵던 **"선제가 틀렸다"는 반증**을 자연스럽게 잡아낸다.
- 원안에 있던 억지스러운 "태동 전/후 게시물 효율 비교(M8)"는 이걸로 **대체하고 폐기.**
- 이유: "잘되면 전략 덕, 안 되면 실행 탓"이면 검증이 아니라 변명이다. 선제가 **틀렸다는 답도 나올 수 있어야** 진짜 검증. 이 교차 판정이 그걸 보장한다.

### 2.5 문턱값(%p) = 감 아님, 작년 노이즈에서 역산
"몇 %p 이상이면 성공"의 기준선은 **Phase 0에서** 작년·재작년 데이터의 "커뮤니케이션이 약했던 해에도 브랜드 검색이 그냥 들쭉날쭉하던 폭(노이즈)"을 먼저 재고, 그 노이즈를 넘는 선으로 잡는다. 우리한테 유리한 쉬운 기준을 몰래 까는 걸 방지.

### 2.6 Q0 외부요인 = 자동감지 X, 수동 플래그
할인·플랫폼 기획전·품절 등 우리가 만들지 않은 판매 변동은 숫자를 흔든다(가짜 초록불/빨간불 둘 다). → 자동 감지는 하지 않고, 해당 구간만 **수동 체크박스/게이트 리뷰 플래그**로 표시해 판정에서 걸러낸다. "우리는 가격 안 올린다"는 반론은 기각됨(할인·플랫폼 프로모션도 왜곡함).

### 2.7 진단 = 두 갈래, 곱하지 않고 나란히
판정(선호가 떴나)은 검색·조회로 하고, **원인 진단은 인스타그램으로**(선제 커뮤니케이션 대부분이 인스타에서 이뤄지므로). 두 갈래를 한 화면에 나란히 놓고 **눈으로 대조:**
- ① 콘텐츠 반응(인스타 좋아요+댓글) ② 브랜드 검색·조회
- 인스타는 외부 게시물의 조회·전환을 추적 불가(API가 좋아요+댓글만 줌) → **두 지표를 곱하거나 비율 계산하지 않고** 두 곡선의 크기만 나란히 본다.
  - 반응↓ + 검색↓ = 콘텐츠 문제
  - 반응↑ + 검색↑ = 잘 된 것
  - 반응↑ + 검색→ = 브랜드 각인 점검 대상
- 실행량(게시율) 자체는 늘 최소한은 하므로 "안 올렸다" 문제는 거의 없다고 봄.
- 인스타그램 전환 추적은 **완전히 제거**(추적 불가라서). 콘텐츠 개선이 필요할 땐 관련 해시태그 인기 콘텐츠 참고 — 우리 카테고리 해시태그는 몇 개 안 돼서 API 한도(7일 30개) 안 걸림 → **자동 수집 가능**.

### 2.8 운영·구조·비용
- 데이터는 **주 1회 자동 갱신**하되, **의사결정은 월말 게이트 3회(9·10·11월)에서만.** 매주 숫자에 반응하면 노이즈에 휘둘림.
- 각 게이트에서 얻은 학습("얼마나 일찍이 충분히 일찍인가")을 다음 카테고리에 적용 → 가장 다듬어진 판단을 매출이 제일 큰 **12월 롱·방한화**에 씀.
- 구조: 서버·DB 없음. 파이썬 수집 스크립트가 무료 공개 API(네이버 데이터랩·검색광고·기상청·인스타그램 Graph)와 내부 데이터를 읽어 **JSON 생성 → 정적 단일 웹페이지가 로드.** 외부 비용 0원.

### 2.9 한계 (명시할 것)
- 인과를 증명하지 않는다. "선호 지표가 의도한 순서(태동 전 상승 → 태동 시점 우위 → 피크 유지)로 움직였나"까지만 본다.
- 첫 시즌은 기준값을 과거 데이터로만 잡아 "대략 맞았나/틀렸나" 수준의 방향 판정. 정밀 판정은 데이터 쌓인 다음 시즌부터.

---

## 3. 현재 만들어진 것 (프로토타입 v1)

- **파일:** `dx-26fw-control-tower.html` — 순수 HTML/CSS/JS 단일 파일, **외부 의존성 0.**
- **차트:** Chart.js를 쓰지 않고 **인라인 SVG로 직접 그린다.** (이유: CDN 차단 환경/배포처에서 안 깨지게. 라인차트·스파크라인·태동/피크 마커·호버 툴팁 전부 자작 SVG.) → 재디자인 시에도 이 무의존 방침 유지 권장.
- **폰트:** Pretendard(jsdelivr CDN) + 시스템 폴백 스택(`Apple SD Gothic Neo, Malgun Gothic, system-ui`). CDN 막혀도 폴백으로 깨지지 않음.

### 3.1 화면 구조 (4개 섹션)
1. **관제탑 헤더 (hero):** 큰 숫자 대신 **"이번 주 판정" 한 줄**(예: `경량 성공 · 숏 경고 — 태동 우위가 피크에서 소멸 · 미드 진행 · 롱 대기`) + 다음 게이트 D-day. CEO 3초 판독용.
2. **릴레이 상태보드:** `경량 → 숏 → 미드 → 롱` 4개 카드를 화살표로 이은 체인. 각 카드 = 신호등 + **태동/피크 두 다리(교차 판정 미니)** + KPI(브랜드검색 YoY / SoS / 리드타임). 카드 클릭 = 해당 카테고리 선택.
3. **워크스페이스 (2열):**
   - 좌: **타이밍 트래커** — 올해(실선) vs 작년(점선) 브랜드 선호 + 카테고리 전체 수요(옅은 선). **달력 / 태동일 정렬 토글.** 태동(세로 실선)·피크(세로 점선) 마커. 마우스 호버 시 값 툴팁.
   - 우: **두 시점 교차 판정 패널** — 태동 시점 ±%p(통과/미달) + 피크 시점 ±%p(통과/미달) + 판정 문구 + **3상태 매트릭스**(해당 결과 칸 점등).
4. **진단 패널:** 좌 콘텐츠 반응(인스타: 게시율·제품명 표기율·좋아요+댓글 스파크라인) / 우 브랜드 검색·조회(브랜드검색·SoS·내부조회 스파크라인) 2열 + "인스타 전환 추적 안 함, 나란히 대조" 주석 + 최근 멘션 게시물 테이블.

### 3.2 인터랙션
- 상태보드 카드 클릭 → 워크스페이스 차트·판정 패널·진단 패널이 전부 그 카테고리로 갱신.
- 달력/태동일 정렬 토글 → 차트 X축 라벨·작년 곡선·마커 위치 재계산.
- 차트 호버 → 세로 가이드 + 올해/작년/전체수요 값 툴팁.
- 반응형: `≤900px` 2열→1열 스택, `≤560px` 카드 1열.

### 3.3 목업 시나리오 (의도적으로 4개 상태 다 노출)
- **경량 = 성공**(GREEN, 태동○·피크○)
- **숏 = 경고**(ALERT, 태동 +16%p○ / 피크 −5%p✗) ← 기본 선택. "선제는 했고 콘텐츠도 괜찮았는데 피크에서 밀린" 케이스를 보여주기 위함.
- **미드 = 진행 중**(AMBER, 태동○ / 피크 판정 대기)
- **롱 = 대기**(WAIT, 아직 노출 전 → 차트에 올해 선이 시작 구간만, 마커 없음)

---

## 4. 현재 디자인 토큰 (참고용 — 재디자인 대상)

> 현재 방향은 "겨울 아우터 관제탑"에 맞춘 **바크브라운 + 올리브 + 차가운 스틸블루**의 계기판/콘솔 미감이었으나, **사용자가 만족하지 못함.** 자유롭게 다시 잡되 아래 두 원칙은 지킬 것:
> (1) **가시성 최우선** — CEO가 3초에 상태 판독. (2) **신호등 색은 정보다** — 성공/경고/실패/대기/교란을 색으로 구분, 장식으로 쓰지 말 것.

```css
:root{
  --paper:#F2EEE4; --card:#FBF9F3; --ink:#221E17; --ink-soft:#6A6355; --ink-faint:#928A7A;
  --line:#E3DBCB; --line-strong:#D3C8B2;
  --bark1:#2E2620; --bark2:#4A3A2C;      /* hero gradient */
  --olive:#6F7237; --steel:#3E5C6B;       /* brand accent / instrument accent */
  /* signal system = information, not decoration */
  --go:#4C6B3F; --amber:#B0801F; --alert:#A24B36; --wait:#948B7C; --disrupt:#455A70;
  --go-bg:#E6EDE0; --amber-bg:#F2E8CD; --alert-bg:#F1DFD7; --wait-bg:#ECE7DC; --disrupt-bg:#E0E6EC;
}
```

신호 매핑: `go`=성공, `amber`=진행중/주의, `alert`=경고/실패, `wait`=대기, `disrupt`=Q0 외부요인 교란(판정 보류, 성패 축과 별개).

**주의(디자인 함정):** "크림 배경(#F4F1EA류) + 세리프 + 테라코타(#D97757)" 조합은 AI가 흔히 뽑는 클리셰다. 현재도 크림 계열이라 이 냄새가 있음 — 재작업 시 이 클러스터에서 벗어나는 걸 권장. 한국어 UI이므로 세리프보다 잘 만든 한글 산세리프(Pretendard 등)가 정답.

---

## 5. 데이터 스키마 (`CATS` 객체) — 실제 데이터 연결 지점

HTML `<script>` 안 `CATS` 객체의 목업 값만 실제 값으로 갈아끼우면 그대로 작동한다. 구조:

```js
const CATS = {
  light: {                                    // key: light | short | mid | long
    idx: 1,                                    // 릴레이 순번 (1~4)
    name: "경량 다운",                          // 카드 제목
    range: "활성 8/04 – 10/05",                 // 활성 기간 라벨
    state: "성공",                              // 신호등 라벨: 성공|경고|진행 중|대기
    sig: "go",                                 // 신호색: go|amber|alert|wait
    onset: { v:"+21%p", thr:"+8%p", mark:"ok" },// 태동 시점: 값 / 문턱 / ok|no|na
    peak:  { v:"+14%p", thr:"+6%p", mark:"ok" },// 피크 시점: 값 / 문턱 / ok|no|na
    kpi: { bs:"×1.24", bsT:"up",               // 브랜드검색 YoY (+추세 up|dn|fl)
           sos:"24.1%", sosT:"up",             // 검색 점유율 (+추세)
           lead:"+31일" },                      // 리드타임(노출이 태동보다 며칠 앞섰나)
    verdictKey: "success",                     // 매트릭스 점등: success|warn|fail|progress|wait
    tracker: {
      cal:      ["9/15","9/22", ...],          // 달력 X축 라벨 (주간)
      thisY:    [24,34,49, ...],               // 올해 브랜드 선호 지수 (null 허용 = 데이터 없음)
      lastY:    [16,20,29, ...],               // 작년 브랜드 선호 지수 (달력 정렬)
      demand:   [22,31,45, ...],               // 카테고리 전체 수요 (컨텍스트)
      alignLbl: ["-3주","-2주","-1주","태동","+1주", ...], // 태동일 정렬 X축 라벨
      lastYAlign:[20,29,40, ...],              // 작년 선호 (태동일 정렬 버전 = 태동일 겹쳐 시프트)
      onsetIdx: 3,                             // 태동 마커 인덱스 (null = 아직 태동 전)
      peakIdx:  5                              // 피크 마커 인덱스 (null = 아직 피크 전)
    },
    verdict: { tag:"...", body:"...", cls:"go" },// 판정 패널 문구 / 색(go|no|na)
    diag: {
      ok: true,                                // 진단 배너 정상 여부
      text: "...",                             // 진단 요약 (HTML 허용, <b> 강조)
      postRate: 88,                            // 게시율 % (막대)
      nameRate: 74,                            // 제품명 표기율 % (막대)
      resp:  [40,52,66, ...],                  // 좋아요+댓글 스파크라인
      brand: [28,40,55, ...],                  // 브랜드검색 스파크라인
      view:  [30,43,58, ...]                   // 내부 조회 스파크라인
    }
  },
  short: { ... }, mid: { ... }, long: { ... }
};
```

- 지수값은 0~100 스케일(차트 Y축 max=110 고정). 실제 연결 시 정규화 필요.
- `null`은 "해당 주에 데이터 없음" → 차트가 선을 끊어 그림(롱=노출 전 구간 등).
- 매트릭스 3행 정의는 `MATRIX_ROWS` 상수에 있음(태동✗=선제 실패 / 태동○·피크✗=경고 / 태동○·피크○=성공).

### 실제 데이터 출처
- **내부 조회·판매:** 지식그래프에 있음 → Claude Code CLI로 취득.
- **브랜드/카테고리 검색·SoS:** 네이버 데이터랩·검색광고 API.
- **날씨(태동일 산출 보조):** 기상청 API.
- **인스타 반응·해시태그:** 인스타그램 Graph API.

---

## 6. 재작업 지시 (Fable에게)

1. **이 문서 2장(방법론)을 이해**하고, 그 논리를 UI가 정확히 표현하도록 유지한다. 특히 **두 시점 교차 판정**이 화면에서 가장 기억에 남는 요소여야 한다.
2. `dx-26fw-control-tower.html`을 읽고 **정보 구조·인터랙션·데이터 스키마는 유지**한 채 **비주얼만 다시** 디자인한다.
3. 차트는 **인라인 SVG 무의존 방식 유지** 권장(Chart.js 등 CDN 도입 시 배포 환경 확인 필요).
4. **가시성**을 최우선으로(사용자가 반복해서 강조한 지점). 신호등 색 의미론 유지.
5. 목업 4개 상태(성공/경고/진행/대기)가 모두 잘 보이는지 렌더링해서 확인.

---

## 7. 유일한 미해결 질문 (설계를 닫으려면 확인 필요)

**작년 내부 데이터(조회·판매)가 소급 가능한가?**
- 작년 내부 데이터가 **있으면** → 문턱값을 "내부 조회 기준"까지 넣어 탄탄하게 잡는다.
- 작년치가 **네이버 검색 같은 공개 데이터만 있으면** → 문턱값을 "브랜드 검색 기준"으로만 잡아야 한다.

이 한 가지만 확정되면 방법론 설계는 완전히 닫힌다. (지식그래프에 작년 내부 데이터가 있는지 확인할 것.)

---

*문서 끝. 표시된 모든 수치는 레이아웃 확인용 목업이다.*
